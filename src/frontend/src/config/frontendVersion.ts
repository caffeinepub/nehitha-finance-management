/**
 * Frontend version identifier for deployment verification.
 * This constant is used to verify that the correct frontend build is deployed to production.
 */
export const FRONTEND_VERSION = 'Draft Version 14';
